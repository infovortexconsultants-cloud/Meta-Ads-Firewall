# Vortex Firewall Deployment Guide

## Quick Deployment (5 Minutes)

### Step 1: Server Setup
```bash
# Ubuntu 20.04 LTS recommended
sudo apt update && sudo apt upgrade -y
sudo apt install python3 python3-pip git -y